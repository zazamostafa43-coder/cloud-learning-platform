# AWS Configuration
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# S3 Buckets (Must match Terraform output)
STT_S3_BUCKET=stt-service-storage-dev
TTS_S3_BUCKET=tts-service-storage-dev
DOC_S3_BUCKET=document-reader-storage-dev
CHAT_S3_BUCKET=chat-service-storage-dev
QUIZ_S3_BUCKET=quiz-service-storage-dev

# Event Bus (Kafka)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092 # Change to MSK endpoint for production

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/learning_platform # Change to RDS endpoint for production

# Service URLs for API Gateway
STT_SERVICE_URL=http://localhost:8001
TTS_SERVICE_URL=http://localhost:8002
DOC_SERVICE_URL=http://localhost:8003
CHAT_SERVICE_URL=http://localhost:8004
QUIZ_SERVICE_URL=http://localhost:8005
